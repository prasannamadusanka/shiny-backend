exports.VIEW_FULL_PAYMENTS = "SELECT my_event.event_id, event.date, reserve.payment_notes, invoice.total_amount, invoice.proceed_date, services.package_price FROM my_event INNER JOIN event ON my_event.event_id = event.event_id INNER JOIN services ON event.package_id = services.package_id INNER JOIN reserve ON my_event.event_id = reserve.my_event_id INNER JOIN invoice ON reserve.invoice_id = invoice.invoice_id WHERE my_event.event_status='Finished' AND invoice.payment_status='Paid' AND services.service_provider_id=23";
